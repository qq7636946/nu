<!DOCTYPE html>
<html lang="zh-Hant">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TA Design | Interieurontwerp met Eenheid</title>

  <!-- 核心框架 -->
  <script src="https://cdn.jsdelivr.net/gh/studio-freight/lenis@1.0.19/bundled/lenis.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
  <!-- 引入 SplitType 用於文字波浪效果 -->
  <script src="https://unpkg.com/split-type"></script>

  <!-- Swiper -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
  <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>

  <!-- CSS Reset -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/meyer-reset/2.0/reset.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Marcellus&family=Noto+Sans+TC:wght@200;300;400&display=swap"
    rel="stylesheet">

  <style>
    :root {
      --bg: #f2ede4;
      --dark: #2c2c2c;
      --white: #ffffff;
      --green: #4a5d4e;
      --gold: #b89a6a;
      --font-serif: 'Marcellus', 'Noto Sans TC', serif;
      --font-sans: 'Noto Sans TC', sans-serif;
      --ease-site: cubic-bezier(0.3, 0.7, 0.05, 1);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      cursor: none;
      -webkit-font-smoothing: antialiased;
    }

    body {
      background-color: var(--bg);
      color: var(--dark);
      font-family: var(--font-sans);
      font-weight: 300;
      overflow-x: hidden;
      line-height: 1.7;
    }

    /* 防止文字溢出容器 */
    .split-parent {
      overflow: hidden;
    }

    .line {
      overflow: hidden;
      display: block;
    }

    /* --- Canvas 雜訊特效 & Vignette --- */
    .noise {
      z-index: 9998;
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      pointer-events: none;
      opacity: .035;
      /* 💡 調整這裡的數值來控制雜訊強度 (例如 0.03 ~ 0.15) */
    }

    /* --- 高級游標系統 --- */
    #cursor {
      position: fixed;
      width: 6px;
      height: 6px;
      background: var(--white);
      mix-blend-mode: difference;
      border-radius: 50%;
      pointer-events: none;
      z-index: 10000;
    }

    #cursor-follower {
      position: fixed;
      width: 34px;
      height: 34px;
      border: 0.5px solid var(--white);
      mix-blend-mode: difference;
      border-radius: 50%;
      pointer-events: none;
      z-index: 9999;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* --- 導覽�?--- */
    nav {
      position: fixed;
      top: 0;
      width: 100%;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 50px 150px;
      z-index: 5000;
      mix-blend-mode: difference;
      color: white;
    }

    .nav-logo {
      display: none;
      /* �?Hero 內已有品牌，nav logo 隱藏 */
    }

    .nav-menu {
      display: flex;
      gap: 36px;
      list-style: none;
      font-size: 0.9rem;
      font-family: var(--font-sans);
      letter-spacing: 2px;
    }

    .nav-menu a {
      text-decoration: none;
      color: inherit;

      transition: opacity 0.4s;
    }

    .nav-menu a:hover {
      opacity: 1;
    }

    /* --- Hero --- */
    .hero {
      height: 100vh;
      width: 100%;
      position: relative;
      display: flex;
      flex-direction: column;
      align-items: center;
      overflow: hidden;
    }

    .hero-bg {
      position: absolute;
      inset: 0;
      background: #000;
      transform: scale(1.1);
      transform-origin: center center;
    }

    .hero-bg .bg-video {
      width: 100%;
      height: 100%;
      object-fit: cover;
      opacity: 0.8;
    }

    .hero-bg .bg-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(to bottom,
          rgba(0, 0, 0, 0.1) 0%,
          rgba(0, 0, 0, 0.45) 100%);
    }

    /* 品牌 Logo 區 �?fixed 固定在頂部置�?*/
    .hero-brand {
      position: fixed;
      top: 5%;
      left: 50%;
      transform: translateX(-50%);
      text-align: center;
      z-index: 4999;
    }

    .hero-brand img {
      height: clamp(60px, 10vw, 100px);
      /* brightness(0) 轉黑 �?invert(53%) 轉為中灰 �?sepia(65%) 淡農暖色�?�?�?A89A85 */
      filter: brightness(0) invert(53%) sepia(65%);
      display: block;
      margin: 0 auto;
    }

    .hero-brand-tagline {
      font-family: var(--font-serif);
      font-size: 0.8rem;
      letter-spacing: 0.45em;
      color: rgba(255, 255, 255, 0.5);
      text-transform: uppercase;
      margin-top: 12px;
    }

    /* 英文副標 �?中央左對�?*/
    .hero-en-subtitle {
      position: absolute;
      top: 38%;
      left: 50%;
      transform: translateX(-50%);
      text-align: left;
      width: 70%;
      z-index: 2;
    }

    .hero-en-subtitle p {
      font-family: var(--font-serif);
      font-size: clamp(1.4rem, 3.5vw, 2.3rem);
      letter-spacing: 0.12em;
      color: rgba(255, 255, 255, 0.92);
      text-transform: uppercase;
      line-height: 1.4;
      text-align: center;
      padding-bottom: 20px;
    }

    /* 分隔�?*/
    .hero-divider {
      width: 1px;
      height: 250px;
      background: rgba(255, 255, 255, 0.4);
      margin: auto;
    }

    /* 下方文字區 */
    .hero-bottom-text {
      position: absolute;
      bottom: 100px;
      left: 50%;
      transform: translateX(-50%);
      width: 70%;
      display: flex;
      flex-direction: column;
      gap: 8px;
      opacity: 1;
      z-index: 2;
      text-align: center;
    }

    .hero-bottom-text .bottom-main {
      font-family: var(--font-sans);
      font-size: clamp(1.1rem, 2.5vw, 1.5rem);
      color: white;
      font-weight: 300;
      letter-spacing: 0.1em;

    }

    .hero-bottom-text .bottom-sub {
      font-family: var(--font-sans);
      font-size: 0.8rem;
      color: rgba(255, 255, 255, 0.6);
      font-weight: 200;
      letter-spacing: 0.06em;
      line-height: 2;
    }


    .btn-pill {
      background: var(--green);
      color: white;
      padding: 14px 32px;
      border-radius: 50px;
      text-decoration: none;
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 2px;
      display: flex;
      align-items: center;
      gap: 12px;
      border: none;
      outline: none;
    }

    /* --- About (Section 2) --- */
    .about {
      padding: 0 0 120px;
      /* removed background: var(--bg) to allow body bg animation */
      position: relative;
      overflow: hidden;
    }

    /* 大裝飾標題跑馬燈 */
    .about-watermark {
      width: 100vw;
      overflow: hidden;
      padding: 40px 0 50px;
      pointer-events: none;
      user-select: none;
      display: flex;
    }

    .watermark-track {
      display: flex;
      flex-shrink: 0;
      animation: marquee 35s linear infinite;
    }

    .watermark-text {
      font-family: var(--font-serif);
      font-size: clamp(5rem, 13vw, 14rem);
      font-weight: 400;
      color: #e5ddd4;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      line-height: 1;
      white-space: nowrap;
      padding-right: 3vw;
    }

    @keyframes marquee {
      0% {
        transform: translateX(0);
      }

      100% {
        transform: translateX(-50%);
      }
    }

    /* 三欄 grid */
    .about-grid {
      display: grid;
      grid-template-columns: 1fr 1.2fr 1fr;
      gap: 60px;
      padding: 0 5%;
      align-items: start;
    }

    /* 左欄 */
    .about-left {
      padding-top: 60px;
    }

    .about-tagline {
      font-family: var(--font-serif);
      font-size: clamp(1.2rem, 2.5vw, 1.8rem);
      font-weight: 400;
      line-height: 1.25;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      color: var(--dark);
      margin-bottom: 36px;
    }

    .about-subtitle-zh {
      font-family: var(--font-sans);
      font-size: 0.95rem;
      font-weight: 300;
      color: var(--dark);
      letter-spacing: 0.08em;
      opacity: 0.8;
    }

    /* 中欄 �?圖片 */
    .about-center {
      position: relative;
    }

    .about-img-mask {
      width: 100%;
      overflow: hidden;
      clip-path: inset(100% 0 0 0);
    }

    .about-img-mask img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transform: scale(1.1);
      display: block;
    }

    /* 圖片底部標題 */
    .about-img-caption {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      background: linear-gradient(transparent, rgba(0, 0, 0, 0.6));
      padding: 40px 24px 20px;
      text-align: center;
    }

    .about-img-caption .cap-en {
      font-family: var(--font-serif);
      font-size: 0.85rem;
      letter-spacing: 0.25em;
      text-transform: uppercase;
      color: rgba(255, 255, 255, 0.9);
      margin-bottom: 4px;
    }

    .about-img-caption .cap-zh {
      font-family: var(--font-sans);
      font-size: 0.8rem;
      letter-spacing: 0.15em;
      color: rgba(255, 255, 255, 0.7);
      font-weight: 200;
    }

    /* 右欄 */
    .about-right {
      padding-top: 120px;
    }

    .about-label {
      font-family: var(--font-serif);
      font-size: 0.8rem;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: var(--dark);
      opacity: 0.5;
      margin-bottom: 20px;
    }

    .about-body {
      font-family: var(--font-sans);
      font-size: 0.88rem;
      font-weight: 300;
      line-height: 2;
      color: var(--dark);
      opacity: 0.85;
    }

    .btn-dark {
      background: var(--dark);
      color: white;
      padding: 16px 40px;
      text-decoration: none;

      display: inline-flex;
      align-items: center;
      gap: 20px;
      font-size: 0.85rem;
      text-transform: uppercase;
      letter-spacing: 2px;
      border: none;
    }

    /* --- Services (Section 3) --- */
    .services {
      min-height: 90vh;
      position: relative;
      /* removed background to allow body bg animation */
      display: flex;
      /* align-items: center; */
      padding-top: 100px;
      justify-content: center;
    }

    .services-title {
      text-align: center;
      z-index: 10;
      max-width: 700px;
      position: relative;
    }

    .services-label {
      font-family: var(--font-serif);
      font-size: 0.6rem;
      letter-spacing: 0.38em;
      text-transform: uppercase;
      color: var(--dark);
      opacity: 0.5;
      margin-bottom: 24px;
    }

    .services-heading {
      font-family: var(--font-serif);
      font-size: clamp(1.4rem, 3vw, 2.4rem);
      color: var(--dark);
      text-align: center;
      text-transform: uppercase;
      line-height: 1.35;
      letter-spacing: 0.05em;
      font-weight: 400;
      margin-bottom: 32px;
    }

    .services-zh {
      font-family: var(--font-sans);
      font-size: 0.85rem;
      font-weight: 300;
      color: var(--dark);
      letter-spacing: 0.1em;
      opacity: 0.7;
    }

    .vertical-line {
      width: 1px;
      height: 60px;
      background: var(--dark);
      opacity: 0.3;
      margin: 0 auto 24px;
    }

    .floating-img {
      position: absolute;
      overflow: hidden;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
    }

    .floating-img img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transform: scale(1.4);
      /* 加大 Zoom In 程度 */
      transition: transform 0.6s ease;
    }

    /* 左上—鷄尾酒 */
    .f-1 {
      width: 230px;
      height: 270px;
      top: 8%;
      left: 5%;
    }

    /* 右上—香橹杯 */
    .f-2 {
      width: 200px;
      height: 145px;
      top: 6%;
      right: 12%;
    }

    /* 中下—義大利�?*/
    .f-3 {
      width: 340px;
      height: 230px;
      bottom: 6%;
      left: 32%;
    }

    /* 右中—餐桌鶴�?*/
    .f-4 {
      width: 265px;
      height: 330px;
      top: 38%;
      right: 8%;
    }

    /* --- Palate (Section 4) --- */
    .palate-section {
      padding: 150px 5% 250px;
      position: relative;
      text-align: center;
    }

    .palate-title {
      font-family: var(--font-serif);
      font-size: clamp(3.5rem, 8vw, 7rem);
      text-transform: uppercase;
      line-height: 1.05;
      letter-spacing: 0.05em;
      color: #dfdbd8;
      position: relative;
      z-index: 2;
      margin-bottom: -50px;
      /* Overlap image */
    }

    .palate-img-wrap {
      position: relative;
      width: 100%;
      max-width: 800px;
      margin: 0 auto;
      z-index: 1;
    }

    .palate-subtitle {
      position: absolute;
      top: 75px;
      /* Inside top of the image */
      left: 50%;
      transform: translateX(-50%);
      font-family: var(--font-sans);
      font-size: 0.85rem;
      font-weight: 300;
      letter-spacing: 0.35em;
      color: rgba(255, 255, 255, 0.7);
      z-index: 3;
      white-space: nowrap;
    }

    .palate-img-mask {
      width: 100%;
      aspect-ratio: 16/9;
      overflow: hidden;
    }

    .palate-img-mask img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .palate-explore {
      position: absolute;
      bottom: -160px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      flex-direction: column;
      align-items: center;
      z-index: 3;
    }

    .explore-line {
      width: 1px;
      height: 130px;
      background: rgba(255, 255, 255, 0.3);
      margin-bottom: 24px;
    }

    .explore-text {
      font-family: var(--font-sans);
      font-size: 0.8rem;
      letter-spacing: 0.25em;
      color: rgb(255, 255, 255);
    }

    /* ════════════════════════════════
             SCROLL PROJECTS (整合段落)
        ════════════════════════════════ */
    .scroll-trigger {
      position: relative;
      height: 700vh;
    }

    .scroll-screen {
      position: sticky;
      top: 0;
      width: 100%;
      height: 100vh;
      overflow: hidden;
    }

    /* ════════════════════════════════
             2026 ELEGANT MENU OVERLAY
       ════════════════════════════════ */
    .menu-overlay {
      position: fixed;
      inset: 0;
      z-index: 9000;
      display: flex;
      visibility: hidden;
      /* clip-path used for swipe down animation */
    }

    .menu-right {
      width: 60vw;
      height: 100vh;
      background: #111111;
      color: #dfdbd8;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 0 10vw;
      position: relative;
    }

    .menu-close {
      position: absolute;
      top: 3.2rem;
      right: 3rem;
      font-family: var(--font-sans);
      font-size: 0.8rem;
      letter-spacing: 0.2em;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 10px;
      opacity: 0.5;
      transition: opacity 0.4s ease;
      overflow: hidden;
    }

    .menu-close:hover {
      opacity: 1;
    }

    .menu-links {
      list-style: none;
      padding: 0;
      margin: 0;
      display: flex;
      flex-direction: column;
      gap: 3.5rem;
    }

    .menu-links li a {
      text-decoration: none;
      color: #dfdbd8;
      display: flex;
      align-items: baseline;
      gap: 2.5rem;
      position: relative;
    }

    /* Hover logic - dim others, slide current */
    .menu-links:hover li {
      opacity: 0.25;
      transition: opacity 0.4s ease;
    }

    .menu-links li:hover {
      opacity: 1;
    }

    .menu-links .en {
      font-family: var(--font-serif);
      font-size: clamp(2rem, 5vw, 4.5rem);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      transition: transform 0.6s cubic-bezier(0.19, 1, 0.22, 1);
    }

    .menu-links .zh {
      font-family: var(--font-sans);
      font-size: 1.05rem;
      font-weight: 300;
      letter-spacing: 0.4em;
    }

    .menu-links li:hover .en {
      transform: translateX(30px);
    }

    .menu-socials {
      position: absolute;
      bottom: 3.5rem;
      left: 10vw;
      display: flex;
      gap: 2.5rem;
      font-family: var(--font-sans);
      font-size: 0.7rem;
      letter-spacing: 0.2em;
      color: rgba(255, 255, 255, 0.4);
    }

    .menu-socials .s-label {
      color: rgba(255, 255, 255, 0.2);
    }

    .menu-socials a {
      color: inherit;
      text-decoration: none;
      transition: color 0.3s;
    }

    .menu-socials a:hover {
      color: #dfdbd8;
    }

    @media (max-width: 768px) {
      .menu-left {
        display: none;
      }

      .menu-right {
        width: 100vw;
        padding: 0 8vw;
      }

      .menu-links {
        gap: 2.5rem;
      }

      .menu-links .en {
        font-size: 2.2rem;
      }

      .menu-links .zh {
        font-size: 0.85rem;
      }

      .menu-socials {
        bottom: 2rem;
        flex-direction: column;
        gap: 1rem;
      }
    }

    /* removed duplicate bg-zoom definition here */

    .page-label {
      position: absolute;
      top: 1.5rem;
      left: 2rem;
      color: white;
      font-size: 0.8rem;
      font-family: var(--font-sans);
      letter-spacing: 0.32em;
      text-transform: uppercase;
      z-index: 30;
      opacity: 0;
    }

    .all-projects-btn {
      position: absolute;
      bottom: 2rem;
      left: 50%;
      transform: translateX(-50%);
      color: white;
      font-size: 0.8rem;
      font-family: var(--font-sans);
      letter-spacing: 0.25em;
      text-transform: uppercase;
      z-index: 30;
      opacity: 0;
      border: 1px solid rgba(255, 255, 255, 0.3);
      border-radius: 100px;
      padding: 0.6rem 1.6rem;
      white-space: nowrap;
      transition: 0.3s;
    }

    .all-projects-btn:hover {
      background: white;
      color: black;
    }

    /* 背景圖片分層 */
    .bg-zoom {
      position: absolute;
      inset: 0;
      overflow: hidden;
      transform: scale(0.4);
      transform-origin: center center;
      will-change: transform;
    }

    .bg-zoom img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .bg-iris {
      position: absolute;
      inset: 0;
      margin: auto;
      overflow: hidden;
      height: 0%;
      width: 100%;
      will-change: height;
    }

    .bg-iris img {
      position: absolute;
      top: 50%;
      left: 0;
      transform: translateY(-50%);
      width: 100%;
      height: 100vh;
      object-fit: cover;
      display: block;
    }

    /* 資訊卡片 */
    .info-card {
      position: absolute;
      inset: 0;
      margin: auto;
      z-index: 20;
      background: white;
      width: 320px;
      height: 440px;
      overflow: hidden;
      will-change: height;
      border-radius: 2px;
    }

    .card-texts {
      position: relative;
      height: 230px;
      overflow: hidden;
      text-transform: uppercase;
    }

    .card-panel {
      position: absolute;
      inset: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 2rem 1.5rem 1rem;
      background: white;
      opacity: 0;
      pointer-events: none;
    }

    .card-panel.is-first {
      opacity: 1;
      pointer-events: auto;
    }

    .card-panel h2 {
      font-family: var(--font-serif);
      font-size: 1.4rem;
      font-weight: 300;
      color: var(--dark);
      margin-bottom: 0.4rem;
      white-space: nowrap;
    }

    .card-panel .cat {
      font-size: 0.9rem;
      letter-spacing: 0.2em;
      color: #000000;
      text-transform: uppercase;
      margin-bottom: auto;
    }

    .card-panel .view-link {
      font-size: 0.8rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--dark);
      text-decoration: none;
      border-bottom: 1px solid var(--dark);
      padding-bottom: 3px;
      margin-bottom: 1rem;
      transition: opacity 0.3s;
    }

    .card-panel .view-link:hover {
      opacity: 0.6;
    }

    .card-thumb-area {
      position: relative;
      width: 100%;
      height: 210px;
      overflow: hidden;
      flex-shrink: 0;
    }

    .card-thumb {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      overflow: hidden;
      will-change: height;
    }

    .card-thumb img {
      width: 100%;
      height: 210px;
      object-fit: cover;
      display: block;
      position: absolute;
      bottom: 0;
      left: 0;
    }

    /* --- Quote --- */
    .quote {
      height: 100vh;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      text-align: center;
    }

    .quote-bg {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('images/bg2.jpg') center/cover;
      z-index: -1;
      transform: scale(1.3);
    }

    .quote-watermark {
      position: absolute;
      top: 0px;
      left: 0;
      width: 100vw;
      overflow: hidden;
      display: flex;
      pointer-events: none;
      z-index: 1;
    }

    .watermark-track-quote {
      display: flex;
      flex-shrink: 0;
      animation: marquee 35s linear infinite;
    }

    .watermark-text-quote {
      font-family: var(--font-serif);
      font-size: 240px;
      color: rgba(255, 255, 255, 0.15);
      white-space: nowrap;
      text-transform: uppercase;
      padding-right: 5vw;
      line-height: 1;
    }

    .quote-content {
      position: relative;
      z-index: 2;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1.5rem;
      margin-top: -5vh;
    }

    .quote-title {
      font-family: var(--font-serif);
      font-size: clamp(2.5rem, 5vw, 4.5rem);
      color: #bfa780;
      font-weight: 400;
      line-height: 1.15;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .quote-zh {
      font-family: var(--font-sans);
      font-size: 0.85rem;
      color: #dfdbd8;
      letter-spacing: 0.3em;
      opacity: 0.9;
      font-weight: 300;
    }

    .quote-en-small {
      margin-top: 0.5rem;
      border-bottom: 2px solid rgba(191, 167, 128, 0.4);
      padding-bottom: 6px;
      display: inline-block;
    }

    .quote-en-small .text {
      font-family: var(--font-serif);
      font-size: 0.7rem;
      letter-spacing: 0.25em;
      color: #bfa780;
      opacity: 0.9;
      text-transform: uppercase;
    }

    /* --- Stories --- */
    .stories {
      padding: 100px 5%;
      background-color: var(--bg);
      position: relative;
    }

    .stories-header {
      display: flex;
      justify-content: space-between;
      border-top: 1px solid rgba(0, 0, 0, 0.2);
      padding-top: 20px;
      margin-bottom: 60px;
      font-size: 0.8rem;
      letter-spacing: 1px;
    }

    .stories-header .title {
      text-transform: uppercase;
      font-weight: 400;
    }

    .stories-header a {
      color: var(--dark);
      text-decoration: none;
      border-bottom: 1px solid var(--dark);
      padding-bottom: 2px;
      text-transform: uppercase;
      transition: opacity 0.3s;
    }

    .stories-header a:hover {
      opacity: 0.6;
    }

    .swiper.stories-grid {
      width: 100%;
      border-right: 1px solid rgba(0, 0, 0, 0.2);
      overflow: hidden;
    }

    .story-item {
      padding: 0 2vw 0 1.5vw;
      border-left: 1px solid rgba(0, 0, 0, 0.2);
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      box-sizing: border-box;
      opacity: 0;
      /* GSAP 起始動畫狀�?*/
    }

    .story-img-box {
      width: 100%;
      height: 250px;
      display: flex;
      align-items: flex-end;
      margin-bottom: 25px;
      overflow: hidden;
    }

    .story-img-box img {
      max-width: 100%;
      max-height: 100%;
      object-fit: contain;
      object-position: left bottom;
      transform-origin: left bottom;
      transform: scale(1.15);
      will-change: transform;
    }

    .story-meta {
      display: flex;
      justify-content: space-between;
      font-size: 0.7rem;
      margin-bottom: 12px;
      opacity: 0.9;
    }

    .story-title {
      font-size: 0.8rem;
      text-transform: uppercase;
      font-weight: 400;
      line-height: 1.5;
      padding-bottom: 20px;
    }

    .stories-nav {
      display: flex;
      justify-content: flex-end;
      align-items: center;
      padding-right: 20px;
      margin-top: 30px;
      font-size: 0.8rem;
      gap: 15px;
    }

    .stories-nav-divider {
      width: 1px;
      height: 15px;
      background: rgba(0, 0, 0, 0.3);
    }


    /* --- Footer --- */
    .footer {
      padding: 180px 8% 60px;
      position: relative;
    }

    .contact-grid {
      display: grid;
      grid-template-columns: 1.1fr 1fr;
      gap: 100px;
      margin-bottom: 180px;
    }

    .contact-left-info {
      padding-right: 20px;
    }

    .footer-logo {
      height: clamp(40px, 6vw, 60px);
      margin-bottom: 40px;
      object-fit: contain;
    }

    .footer-desc .en {
      font-family: var(--font-serif);
      font-size: clamp(1.2rem, 2vw, 1.6rem);
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--dark);
      margin-bottom: 20px;
      line-height: 1.4;
    }

    .footer-desc .zh {
      font-family: var(--font-sans);
      font-size: 0.95rem;
      letter-spacing: 0.15em;
      color: var(--dark);
      opacity: 0.7;
      line-height: 1.8;
      font-weight: 300;
    }

    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      margin-bottom: 25px;
    }

    .form-row .form-input {
      margin-bottom: 0;
    }

    .form-input {
      width: 100%;
      background: #e9e4db;
      border: none;
      padding: 22px;
      margin-bottom: 25px;
      font-family: inherit;
      font-weight: 300;
      font-size: 0.9rem;
      outline: none;
      color: var(--dark);
    }

    .form-input::placeholder {
      color: rgba(44, 44, 44, 0.5);
    }

    .form-input:focus {
      border: 1px solid var(--green);
    }

    .footer-info {
      display: flex;
      justify-content: space-between;
      border-top: 0.5px solid #ccc;
      padding-top: 45px;
      font-size: 0.8rem;
      opacity: 0.7;
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    .footer-marquee {
      width: 100vw;
      overflow: hidden;
      margin-top: 80px;
      margin-bottom: -60px;
      position: relative;
      left: 50%;
      transform: translateX(-50%);
      pointer-events: none;
      user-select: none;
      display: flex;
    }

    .footer-marquee-text {
      font-family: var(--font-serif);
      font-size: 16vw;
      color: #e9e4db;
      line-height: 0.8;
      font-weight: 300;
      white-space: nowrap;
      padding-right: 2vw;
      text-transform: uppercase;
    }

    @media (max-width: 1024px) {
      .about {
        grid-template-columns: 1fr;
        gap: 60px;
      }

      .about-right {
        padding-top: 0;
      }

      .floating-img {
        display: none;
      }

      .contact-grid {
        grid-template-columns: 1fr;
        gap: 60px;
      }
    }

    @media (max-width: 768px) {
      .form-row {
        grid-template-columns: 1fr;
        gap: 0;
      }

      .form-row .form-input {
        margin-bottom: 25px;
      }

      .form-row:last-child .form-input:last-child {
        margin-bottom: 25px;
      }
    }

    /* ─────────────────────────────────────
       LOADING SCREEN
    ──────────────────────────────────────*/
    #loader {
      position: fixed;
      inset: 0;
      z-index: 99999;
      background: #0e0e0e;
      display: flex;
      align-items: center;
      justify-content: center;
      pointer-events: all;
      will-change: clip-path;
    }

    .loader-logo-wrap {
      overflow: hidden;
      line-height: 0;
    }

    .loader-logo {
      height: clamp(50px, 8vw, 80px);
      filter: brightness(0) invert(53%) sepia(65%);
      display: block;
      clip-path: inset(100% 0 0 0);
    }
  </style>
</head>

<body>

  <!-- ══�?LOADING SCREEN ══�?-->
  <div id="loader">
    <div class="loader-logo-wrap">
      <img src="images/logo2.svg" alt="" class="loader-logo" id="loaderLogo">
    </div>
  </div>

  <!-- 雜訊特效 (Canvas Noise) -->
  <canvas id="noise" class="noise"></canvas>

  <!-- 自定義游�?-->
  <div id="cursor"></div>
  <div id="cursor-follower"></div>

  <!-- 導覽�?-->
  <nav>
    <ul class="nav-menu">
      <li><a href="#" class="mag">暮色札記</a></li>
      <li><a href="#" class="mag">本節故事</a></li>
      <li><a href="#" class="mag">食光清單</a></li>
      <li><a href="#" class="mag">尋味真色</a></li>
    </ul>
    <div class="nav-right">
      <span class="mag" style="font-size: 0.9rem; letter-spacing: 1px;padding-right: 20px;">RESERVATIONS / 預約訂位</span>
      <span class="mag" id="menuOpenBtn" style="font-size: 0.9rem; letter-spacing: 1px; cursor: pointer;">MENU /
        啟扉之味</span>
    </div>
  </nav>

  <!-- SECTION 1: HERO -->
  <section class="hero">
    <div class="hero-bg">
      <video class="bg-video" autoplay muted playsinline src="images/bk.mp4"></video>
      <div class="bg-overlay"></div>
    </div>

    <!-- 品牌 Logo �?上方置中 -->
    <div class="hero-brand">
      <img src="images/logo.svg" alt="暮色食光">
      <p class="hero-brand-tagline">Meterseven</p>
    </div>

    <!-- 英文副標 �?中央左對�?-->
    <div class="hero-en-subtitle">
      <p>Authentic Italian Pasta Meets<br>Modern Mixology</p>
      <div class="hero-divider"></div>
    </div>

    <!-- 下方中文說明文字 -->
    <div class="hero-bottom-text">
      <p class="bottom-main">在微醺的午後，找到生活的節�?/p>
      <p class="bottom-sub">對道地義大利風味的致敬�?br>在微醺的午後，找到生活的節奏�?/p>
    </div>
  </section>

  <!-- SECTION 2: ABOUT -->
  <section class="about">
    <!-- 大裝飾標�?-->
    <div class="about-watermark">
      <div class="watermark-track">
        <div class="watermark-text">SUNSET FLAVORS &mdash; SUNSET FLAVORS &mdash; SUNSET FLAVORS &mdash;</div>
        <div class="watermark-text">SUNSET FLAVORS &mdash; SUNSET FLAVORS &mdash; SUNSET FLAVORS &mdash;</div>
      </div>
    </div>

    <!-- 三欄內容 -->
    <div class="about-grid">
      <!-- 左欄：標�?-->
      <div class="about-left">
        <h2 class="about-tagline">Tipsy Evenings,<br>Timeless Flavors<br>A Toast to the<br>Night.</h2>
        <p class="about-subtitle-zh">微醺的夜，永恒的味道</p>
      </div>

      <!-- 中欄：圖�?-->
      <div class="about-center">
        <div class="about-img-mask">
          <img src="images/f1.jpg" alt="暮色食光">
        </div>
        <div class="about-img-caption">
          <p class="cap-en">Twilight &amp; Taste</p>
          <p class="cap-zh">暮色已至，食味正�?/p>
        </div>
      </div>

      <!-- 右欄：說明文�?-->
      <div class="about-right">
        <p class="about-label">Sip &amp; Relax</p>
        <p class="about-body">
          在這座喧囂的城市裡，我們試圖用一捃温柔的氛氣，勾勒出生活的原點。大概食館不僅是一家餐廠，更是一場關於「食」的實驗。我們深信真正能溫暖人心的，從來不是豪華的裝簾，而是那份紮實、專尿、充滿溫揁的導牙沖味�?/p>
      </div>
    </div>
  </section>

  <!-- SECTION 3: SERVICES -->
  <section class="services">
    <!-- 左上—鷄尾酒 -->
    <div class="floating-img f-1" data-speed="0.04">
      <img src="images/f2.jpg" alt="cocktail">
    </div>
    <!-- 右上—香橹杯 -->
    <div class="floating-img f-2" data-speed="-0.03">
      <img src="images/f2.jpg" alt="wine">
    </div>

    <!-- 中心標題 -->
    <div class="services-title">
      <div class="vertical-line"></div>
      <p class="services-label">Sip &amp; Relax</p>
      <h2 class="services-heading">Liquid Gold &amp; Golden Wheat.<br>The Essential Bite<br>Sunset Flavors</h2>
      <p class="services-zh">在金黃麥浪與琥珀流金之間，找回味蕾最純粹的感動�?/p>
    </div>

    <!-- 中下—義大利�?-->
    <div class="floating-img f-3" data-speed="0.05">
      <img src="images/f5.jpg" alt="pasta">
    </div>
    <!-- 右中—餐桌鶴�?-->
    <div class="floating-img f-4" data-speed="-0.04">
      <img src="images/f4.jpg" alt="table">
    </div>
  </section>

  <!-- SECTION 4: SCROLL PROJECTS ANIMATION (整合進來的特效區�? -->
  <section class="scroll-trigger" id="trigger">
    <div class="scroll-screen">



      <!-- ░░ BG Layer 1: First image �?zooms in ░░ -->
      <div class="bg-zoom" id="bgZoom">
        <img src="images/slider2.jpg" alt="Interior 1">
      </div>

      <!-- ░░ PALATE TEXT OVERLAY ░░ -->
      <style>
        .palate-overlay {
          position: absolute;
          inset: 0;
          z-index: 5;
          pointer-events: none;
        }

        .palate-title-overlay {
          position: absolute;
          bottom: 65%;
          left: 50%;
          transform: translateX(-50%);
          font-family: var(--font-serif);
          font-size: clamp(3.5rem, 8vw, 6rem);
          text-transform: uppercase;
          line-height: 1.05;
          letter-spacing: 0.05em;
          color: #dfdbd8;
          text-align: center;
          white-space: nowrap;
        }

        .palate-subtitle-overlay {
          position: absolute;
          top: 35%;
          left: 50%;
          transform: translateX(-50%);
          font-family: var(--font-sans);
          font-size: 0.85rem;
          font-weight: 300;
          letter-spacing: 0.35em;
          color: rgba(255, 255, 255, 0.7);
          white-space: nowrap;
        }

        .palate-explore-overlay {
          position: absolute;
          top: 70%;
          left: 50%;
          transform: translateX(-50%);
          display: flex;
          flex-direction: column;
          align-items: center;
        }
      </style>
      <div class="palate-overlay" id="palateTextOverlay">
        <div class="palate-title-overlay">
          <div>UNLOCK THE</div>
          <div style="margin-top: -10px;">PALATE</div>
        </div>
        <div class="palate-subtitle-overlay">啟發您的味蕾</div>
        <div class="palate-explore-overlay">
          <div class="explore-line"></div>
          <div class="explore-text">探索</div>
        </div>
      </div>

      <!-- ░░ BG Layer 2: Second image �?iris open ░░ -->
      <div class="bg-iris" id="bgIris2">
        <img src="images/slider1.jpg" alt="Interior 2">
      </div>

      <!-- ░░ BG Layer 3: Third image �?iris open ░░ -->
      <div class="bg-iris" id="bgIris3">
        <img src="images/slider3.jpg" alt="Interior 3">
      </div>

      <!-- ░░ Info Card: iris opens on entry ░░ -->
      <div class="info-card" id="infoCard">

        <!-- Text panels -->
        <div class="card-texts">
          <div class="card-panel is-first" id="panel1">
            <h2> Brioche French Toast</h2>
            <p class="cat">微醺晨曦・布里歐法式吐司</p>
            <a class="view-link mag">View Project</a>
          </div>
          <div class="card-panel" id="panel2">
            <h2>Linguine alle Vongole</h2>
            <p class="cat">白酒蒜香・地中海蛤蜊細扁�?/p>
            <a class="view-link mag">View Project</a>
          </div>
          <div class="card-panel" id="panel3">
            <h2>Seared Scallops & Cavia</h2>
            <p class="cat"> 極致・干貝魚子醬金黃寬麵</p>
            <a class="view-link mag">View Project</a>
          </div>
        </div>

        <!-- Thumbnail area -->
        <div class="card-thumb-area">
          <!-- Layer 0: Project 3 (always underneath) -->
          <div class="card-thumb" style="height:210px; z-index:1;">
            <img src="images/p3.jpg" alt="">
          </div>
          <!-- Layer 1: Project 2 -->
          <div class="card-thumb" id="thumb2" style="height:210px; z-index:2;">
            <img src="images/p1.jpg" alt="">
          </div>
          <!-- Layer 2: Project 1 (on top, first visible) -->
          <div class="card-thumb" id="thumb1" style="height:210px; z-index:3;">
            <img src="images/p2.jpg" alt="">
          </div>
        </div>
      </div><!-- /info-card -->

      <div class="all-projects-btn mag" id="allProjectsBtn">Alle Projecten �?/div>

    </div>
  </section>

  <!-- SECTION 5: QUOTE -->
  <section class="quote">
    <div class="quote-bg"></div>
    <div class="quote-watermark">
      <div class="watermark-track-quote">
        <div class="watermark-text-quote">LOCATE US &mdash; LOCATE US &mdash; LOCATE US &mdash; LOCATE US &mdash;</div>
        <div class="watermark-text-quote">LOCATE US &mdash; LOCATE US &mdash; LOCATE US &mdash; LOCATE US &mdash;</div>
      </div>
    </div>
    <div class="quote-content">
      <h2 class="quote-title split-text">IN SEARCH OF<br>TWILIGHT</h2>
      <p class="quote-zh">尋訪暮色</p>
      <div class="quote-en-small">
        <span class="text">SEEKING TWILIGHT</span>
      </div>
    </div>
  </section>

  <!-- SECTION 6: STORIES -->
  <section class="stories">
    <div class="stories-header">
      <span class="title">STORIES <sup>(5)</sup></span>
      <a href="#" class="mag">DISCOVER ALL POSTS</a>
    </div>
    <div class="swiper stories-grid" id="storiesGrid">
      <div class="swiper-wrapper">
        <!-- Story 1 -->
        <div class="swiper-slide story-item">
          <div class="story-img-box">
            <img src="images/01.jpg" alt="Dining Table">
          </div>
          <div class="story-meta">
            <span>August 12, 2026</span>
            <span>( Menu )</span>
          </div>
          <h3 class="story-title">Tasting the Ocean:<br>A Mediterranean Feast</h3>
        </div>
        <!-- Story 2 -->
        <div class="swiper-slide story-item">
          <div class="story-img-box">
            <img src="images/02.jpg" alt="Art of Plating">
          </div>
          <div class="story-meta">
            <span>September 05, 2026</span>
            <span>( Culinary )</span>
          </div>
          <h3 class="story-title">The Art of Plating:<br>Visual Poetry on a Dish</h3>
        </div>
        <!-- Story 3 -->
        <div class="swiper-slide story-item">
          <div class="story-img-box">
            <img src="images/03.jpg" alt="Wine Toast">
          </div>
          <div class="story-meta">
            <span>October 22, 2026</span>
            <span>( Cellar )</span>
          </div>
          <h3 class="story-title">Curating Memories:<br>A Symphony in Glass</h3>
        </div>
        <!-- Story 4 -->
        <div class="swiper-slide story-item">
          <div class="story-img-box">
            <img src="images/04.jpg" alt="Dessert Finishing">
          </div>
          <div class="story-meta">
            <span>November 10, 2026</span>
            <span>( Dessert )</span>
          </div>
          <h3 class="story-title">Sinfonia Dolce:<br>An Exquisite Conclusion</h3>
        </div>
        <!-- Story 5 -->
        <div class="swiper-slide story-item">
          <div class="story-img-box">
            <img src="images/p1.jpg" alt="Chef Creation">
          </div>
          <div class="story-meta">
            <span>December 01, 2026</span>
            <span>( Kitchen )</span>
          </div>
          <h3 class="story-title">Behind the Scenes:<br>Crafting the Experience</h3>
        </div>
      </div> <!-- end swiper-wrapper -->
    </div>
    <div class="stories-nav">
      <span class="mag" id="prevStory">Previous</span>
      <div class="stories-nav-divider"></div>
      <span class="mag" id="nextStory">Next</span>
    </div>
  </section>

  <!-- SECTION 7: FOOTER -->
  <footer class="footer">
    <div class="contact-grid">
      <div class="contact-left-info">
        <img src="images/logo2.svg" alt="Nudot Logo" class="footer-logo">
        <div class="footer-desc">
          <p class="en split-text">Experience the harmony<br>of modern cuisine and<br>timeless ambiance.</p>
          <p class="zh split-text">體驗現代美饌與經典氛圍的完美交融�?br>期待與您分享每一個微醺食光�?/p>
        </div>
      </div>
      <div class="contact-form">
        <div class="form-row">
          <input type="text" placeholder="Name / 姓名" class="form-input">
          <input type="tel" placeholder="Phone / 聯絡電話" class="form-input">
        </div>
        <div class="form-row">
          <input type="text" placeholder="Date / 預約日期 (YYYY-MM-DD)" onfocus="(this.type='date')"
            onblur="(this.type='text')" class="form-input">
          <input type="text" placeholder="Time / 預約時間" class="form-input">
        </div>
        <div class="form-row">
          <input type="number" placeholder="Guests / 用餐人數" class="form-input" min="1">
          <input type="email" placeholder="Email / 聯絡信箱" class="form-input">
        </div>
        <textarea placeholder="Special Requests / 備註或特殊要�? class="form-input" rows="3"></textarea>
        <button class="btn-dark mag">BOOK NOW �?/button>
      </div>
    </div>

    <div class="footer-info">
      <span>Meterseven</span>
      <span>© 2026 暮色食光 Meterseven</span>
      <span>Website by Nudot</span>
    </div>
    <div class="footer-marquee">
      <div class="watermark-track">
        <div class="footer-marquee-text">METERSEVEN &mdash; METERSEVEN &mdash; METERSEVEN &mdash; METERSEVEN
          &mdash;&nbsp;</div>
        <div class="footer-marquee-text">METERSEVEN &mdash; METERSEVEN &mdash; METERSEVEN &mdash; METERSEVEN
          &mdash;&nbsp;</div>
      </div>
    </div>
  </footer>

  <!-- 2026 MOST ELEGANT MENU OVERLAY -->
  <div class="menu-overlay" id="menuOverlay">

    <div class="menu-right" style="width: 100vw; padding: 0 15vw;">
      <div class="menu-close" id="menuCloseBtn">
        CLOSE<span>/ 關閉</span>
      </div>

      <ul class="menu-links">
        <li>
          <a href="#">
            <div class="en">DUSK NOTES</div>
            <div class="zh">暮色札記</div>
          </a>
        </li>
        <li>
          <a href="#">
            <div class="en">THE STORY</div>
            <div class="zh">本節故事</div>
          </a>
        </li>
        <li>
          <a href="#">
            <div class="en">THE MENU</div>
            <div class="zh">食光清單</div>
          </a>
        </li>
        <li>
          <a href="#">
            <div class="en">ESSENTIA</div>
            <div class="zh">尋味真色</div>
          </a>
        </li>
      </ul>

      <div class="menu-socials">
        <span class="s-label">Follow Us</span>
        <a href="#">INSTAGRAM</a>
        <a href="#">FACEBOOK</a>
        <a href="#">LINE OFFICIAL</a>
      </div>
    </div>
  </div>

  <script>
    gsap.registerPlugin(ScrollTrigger);

    // 1. 初始�?Lenis 平滑捲動
    const lenis = new Lenis({ lerp: 0.1, smoothWheel: true });
    function raf(time) { lenis.raf(time); requestAnimationFrame(raf); }
    requestAnimationFrame(raf);

    // Hero 進場動畫
    const heroTl = gsap.timeline({ delay: 0 });
    heroTl
      .from('.hero-brand img', { opacity: 0, y: -12, duration: 0.6, ease: 'power3.out' })
      .from('.hero-brand-tagline', { opacity: 0, duration: 0.4, ease: 'power2.out' }, '-=0.3')
      .from('.hero-en-subtitle p', { opacity: 0, y: 12, duration: 0.6, ease: 'power3.out' }, '-=0.3')
      .from('.hero-divider', { scaleY: 0, duration: 0.5, ease: 'power2.inOut', transformOrigin: 'top center' }, '-=0.2')
      .from('.hero-bottom-text', { opacity: 0, y: 10, duration: 0.5, ease: 'power3.out' }, '-=0.2');

    // Logo 捲動縮小：絲滑縮至最�?50%
    gsap.to('.hero-brand', {
      scale: 0.5,
      transformOrigin: 'center top',
      ease: 'none',
      scrollTrigger: {
        trigger: '.hero',
        start: 'top top',
        end: 'bottom top',   // hero 完全離開畫面時，scale 剛好停在 0.5
        scrub: 1.2,
      }
    });

    // 2. 自定義游�?(加入 mix-blend-mode 自動適應背景)
    const cursor = document.querySelector("#cursor");
    const follower = document.querySelector("#cursor-follower");
    window.addEventListener("mousemove", (e) => {
      gsap.to(cursor, { x: e.clientX, y: e.clientY, duration: 0 });
      gsap.to(follower, { x: e.clientX - 17, y: e.clientY - 17, duration: 0.35, ease: "power2.out" });
    });

    // 3. 磁吸按鈕
    document.querySelectorAll(".mag").forEach(el => {
      el.addEventListener("mousemove", (e) => {
        const rect = el.getBoundingClientRect();
        const x = (e.clientX - rect.left - rect.width / 2) * 0.45;
        const y = (e.clientY - rect.top - rect.height / 2) * 0.45;
        gsap.to(el, { x, y, duration: 0.4 });
      });
      el.addEventListener("mouseleave", () => {
        gsap.to(el, { x: 0, y: 0, duration: 0.7, ease: "elastic.out(1, 0.3)" });
      });
    });

    // 4. 波浪文字特效 (Wave Reveal)
    const initTextAnimations = () => {
      const splitTargets = document.querySelectorAll('.split-text');
      splitTargets.forEach(target => {
        const split = new SplitType(target, { types: 'lines, chars' });
        gsap.set(split.chars, { y: 100, opacity: 0 });
        gsap.to(split.chars, {
          y: 0, opacity: 1,
          duration: 1.2, ease: "power4.out",
          stagger: { amount: 0.8, from: "start" },
          scrollTrigger: {
            trigger: target,
            start: "top 85%",
            toggleActions: "play none none reverse"
          }
        });
      });
    };

    // 5. 基礎區塊圖�?Zoom Out 動態
    const initImageAnimations = () => {
      gsap.to(".hero-bg", {
        scale: 1,
        scrollTrigger: { trigger: ".hero", start: "top top", end: "bottom top", scrub: true }
      });
      gsap.to(".about-img-mask img", {
        scale: 1,
        scrollTrigger: { trigger: ".about", start: "top bottom", end: "bottom top", scrub: true }
      });
      document.querySelectorAll(".floating-img img").forEach(img => {
        gsap.to(img, {
          scale: 1,
          scrollTrigger: { trigger: img.parentElement, start: "top bottom", end: "bottom top", scrub: true }
        });
      });
      gsap.to(".quote-bg", {
        scale: 1,
        scrollTrigger: { trigger: ".quote", start: "top bottom", end: "bottom top", scrub: true }
      });
      gsap.to(".story-img-box img", {
        scale: 1,
        scrollTrigger: { trigger: ".stories", start: "top bottom", end: "bottom top", scrub: true }
      });
    };

    // 6. 強大專案滾動特效 (Iris Reveal & Card Wipe)
    const initProjectsScrollAnimation = () => {
      const E = 'power3.inOut';
      const EF = 'power2.inOut';

      gsap.to('body', {
        backgroundColor: '#514942',
        scrollTrigger: { trigger: '.scroll-trigger', start: 'top 80%', end: 'top 20%', scrub: true }
      });
      gsap.to('body', {
        backgroundColor: '#f2ede4',
        scrollTrigger: { trigger: '.quote', start: 'top 90%', end: 'top 30%', scrub: true }
      });

      gsap.set('#infoCard', { height: 0 });
      const tl = gsap.timeline({
        scrollTrigger: { trigger: '#trigger', start: 'top top', end: 'bottom bottom', scrub: 2.5 }
      });
      tl.to('#bgZoom', { scale: 1, ease: E, duration: 2 }, 0);
      tl.to('#palateTextOverlay', { opacity: 0, ease: EF, duration: 1.2 }, 0);
      tl.to('#pageLabel', { opacity: 1, ease: EF, duration: 1 }, 0.6);
      tl.to('#infoCard', { height: 440, ease: E, duration: 1.8 }, 1.0);
      tl.to('#bgIris2', { height: '100%', ease: E, duration: 1.8 }, 3.8);
      tl.to('#panel1', { opacity: 0, ease: EF, duration: 0.6 }, 4.2);
      tl.to('#panel2', { opacity: 1, ease: EF, duration: 0.6 }, 4.7);
      tl.to('#thumb1', { height: 0, ease: E, duration: 1.4 }, 4.0);
      tl.to('#bgIris3', { height: '100%', ease: E, duration: 1.8 }, 6.5);
      tl.to('#panel2', { opacity: 0, ease: EF, duration: 0.6 }, 6.8);
      tl.to('#panel3', { opacity: 1, ease: EF, duration: 0.6 }, 7.3);
      tl.to('#thumb2', { height: 0, ease: E, duration: 1.4 }, 6.6);
      tl.to('#allProjectsBtn', { opacity: 1, ease: EF, duration: 0.6 }, 8.5);
    };

    // 7. LOADING 退�?+ 啟動總指�?    const loader = document.getElementById('loader');
    const loaderLogo = document.getElementById('loaderLogo');

    function kickOffPage() {
      const heroTitle = new SplitType('.hero-title', { types: 'chars' });
      gsap.set(heroTitle.chars, { y: 150, opacity: 0 });
      gsap.timeline()
        .to(heroTitle.chars, { y: 0, opacity: 1, duration: 2, ease: 'expo.out', stagger: 0.03 })
        .to('.hero-footer', { opacity: 1, y: -20, duration: 1 }, '-=1.5');

      initTextAnimations();
      initImageAnimations();
      initProjectsScrollAnimation();

      gsap.to('.about-img-mask', {
        clipPath: 'inset(0% 0 0 0)', duration: 0.9, ease: 'power3.out',
        scrollTrigger: { trigger: '.about-center', start: 'top 85%' }
      });

      document.querySelectorAll('.floating-img').forEach(img => {
        const speed = parseFloat(img.dataset.speed);
        gsap.to(img, {
          y: () => -500 * speed,
          scrollTrigger: { trigger: '.services', start: 'top bottom', end: 'bottom top', scrub: true }
        });
        gsap.fromTo(img,
          { clipPath: 'inset(100% 0 0 0)' },
          {
            clipPath: 'inset(0% 0 0 0)', duration: 0.85, ease: 'power3.out',
            scrollTrigger: { trigger: img, start: 'top 85%' }
          }
        );
      });

      const swiper = new Swiper('.swiper.stories-grid', {
        slidesPerView: 1.2, spaceBetween: 0, speed: 800, grabCursor: true,
        navigation: { nextEl: '#nextStory', prevEl: '#prevStory' },
        breakpoints: { 768: { slidesPerView: 2.2 }, 1024: { slidesPerView: 4 } }
      });

      gsap.to('.swiper-slide', {
        scrollTrigger: { trigger: '.stories', start: 'top 75%' },
        opacity: 1, duration: 1.2, stagger: 0.15, ease: 'power3.inOut'
      });

      gsap.utils.toArray('.palate-img-mask').forEach(mask => {
        gsap.fromTo(mask,
          { clipPath: 'inset(100% 0 0 0)' },
          {
            clipPath: 'inset(0% 0 0 0)', duration: 0.9, ease: 'power3.out',
            scrollTrigger: { trigger: mask, start: 'top 85%' }
          }
        );
      });

      gsap.utils.toArray('.palate-title, .palate-subtitle, .about-watermark, .about-tagline, .about-body, .services-heading, .services-label, .explore-line, .explore-text, .quote-zh, .quote-en-small').forEach(item => {
        gsap.fromTo(item,
          { y: 60, opacity: 0, clipPath: 'inset(0 0 100% 0)' },
          {
            y: 0, opacity: 1, clipPath: 'inset(0 0 -20% 0)', duration: 0.85, ease: 'power3.out',
            scrollTrigger: { trigger: item, start: 'top 90%' }
          }
        );
      });

      const menuTl = gsap.timeline({ paused: true, defaults: { ease: 'power4.inOut' } });
      gsap.set('#menuOverlay', { clipPath: 'inset(100% 0% 0% 0%)', autoAlpha: 0 });
      gsap.set('.menu-links li, .menu-socials, .menu-close', { y: 40, opacity: 0 });
      menuTl
        .to('#menuOverlay', { autoAlpha: 1, duration: 0.1 })
        .to('#menuOverlay', { clipPath: 'inset(0% 0% 0% 0%)', duration: 1.4 }, 0)
        .to('.menu-links li', { y: 0, opacity: 1, duration: 1.2, stagger: 0.08 }, 0.4)
        .to('.menu-socials, .menu-close', { y: 0, opacity: 1, duration: 1 }, 0.7);

      document.getElementById('menuOpenBtn').addEventListener('click', () => {
        document.body.style.overflow = 'hidden';
        menuTl.play();
      });
      document.getElementById('menuCloseBtn').addEventListener('click', () => {
        document.body.style.overflow = '';
        menuTl.reverse();
      });
    }

    // window.load �?AWW 風格：LOGO 刷入 �?停留 �?整片上刷退�?�?頁面進場
    window.addEventListener('load', () => {
      const exitTl = gsap.timeline({ delay: 0.15 });
      exitTl.to(loaderLogo, {
        clipPath: 'inset(0% 0 0 0)',
        duration: 0.9,
        ease: 'power4.out'
      })
        .to(loader, {
          clipPath: 'inset(0 0 100% 0)',
          duration: 1.05,
          ease: 'power4.inOut',
          delay: 0.55,
          onStart: () => { loader.style.pointerEvents = 'none'; },
          onComplete: () => {
            loader.style.display = 'none';
            kickOffPage();
          }
        });
    });

    // --- Canvas Noise Script ---
    const noiseCanvas = document.getElementById('noise');
    const noiseCtx = noiseCanvas.getContext('2d');

    let noiseWidth, noiseHeight;
    let noiseData = [];
    let noiseFrame = 0;
    let noiseLoopTimeout;

    const createNoise = () => {
      const idata = noiseCtx.createImageData(noiseWidth, noiseHeight);
      const buffer32 = new Uint32Array(idata.data.buffer);
      const len = buffer32.length;
      for (let i = 0; i < len; i++) {
        if (Math.random() < 0.5) {
          buffer32[i] = 0xff000000; // 黑色隨機像素
        } else {
          buffer32[i] = 0xffffffff; // 白色隨機像素
        }
      }
      noiseData.push(idata);
    };

    const paintNoise = () => {
      if (noiseFrame === 9) {
        noiseFrame = 0;
      } else {
        noiseFrame++;
      }
      noiseCtx.putImageData(noiseData[noiseFrame], 0, 0);
    };

    const loopNoise = () => {
      paintNoise();
      noiseLoopTimeout = window.setTimeout(() => {
        window.requestAnimationFrame(loopNoise);
      }, (1000 / 25)); // 設定�?25 楨率的雜訊動�?    };

    const setupNoise = () => {
      noiseWidth = window.innerWidth;
      noiseHeight = window.innerHeight;
      noiseCanvas.width = noiseWidth;
      noiseCanvas.height = noiseHeight;
      noiseData = [];

      for (let i = 0; i < 10; i++) {
        createNoise();
      }
      loopNoise();
    };

    setupNoise();

    window.addEventListener('resize', () => {
      window.clearTimeout(noiseLoopTimeout);
      setupNoise();
    });

    // --- Interactive Line Effect (點下去拉一條線) ---
    const iCanvas = document.createElement('canvas');
    iCanvas.id = 'interactionCanvas';
    Object.assign(iCanvas.style, {
      position: 'fixed',
      top: '0',
      left: '0',
      width: '100vw',
      height: '100vh',
      zIndex: '9997',
      pointerEvents: 'none'
    });
    document.body.appendChild(iCanvas);
    const iCtx = iCanvas.getContext('2d');
    let iw = window.innerWidth;
    let ih = window.innerHeight;
    iCanvas.width = iw * window.devicePixelRatio;
    iCanvas.height = ih * window.devicePixelRatio;
    iCtx.scale(window.devicePixelRatio, window.devicePixelRatio);

    window.addEventListener('resize', () => {
      iw = window.innerWidth;
      ih = window.innerHeight;
      iCanvas.width = iw * window.devicePixelRatio;
      iCanvas.height = ih * window.devicePixelRatio;
      iCtx.scale(window.devicePixelRatio, window.devicePixelRatio);
    });

    let isPulling = false;
    let pointer = { x: iw / 2, y: ih / 2 };
    let points = [];

    const addPoint = (x, y) => {
      points.push({ x, y, age: 0 });
    };

    window.addEventListener('mousedown', (e) => {
      isPulling = true;
      addPoint(e.clientX, e.clientY);
    });
    window.addEventListener('mousemove', (e) => {
      if (!isPulling) return;
      addPoint(e.clientX, e.clientY);
    });
    window.addEventListener('mouseup', () => {
      isPulling = false;
    });
    window.addEventListener('touchstart', (e) => {
      isPulling = true;
      addPoint(e.touches[0].clientX, e.touches[0].clientY);
    });
    window.addEventListener('touchmove', (e) => {
      if (!isPulling) return;
      addPoint(e.touches[0].clientX, e.touches[0].clientY);
    });
    window.addEventListener('touchend', () => {
      isPulling = false;
    });

    const loopInteraction = () => {
      iCtx.clearRect(0, 0, iw, ih);

      for (let i = 0; i < points.length; i++) {
        points[i].age += 1;
      }
      points = points.filter(p => p.age < 50);

      if (points.length > 1) {
        for (let i = 0; i < points.length - 1; i++) {
          iCtx.beginPath();
          iCtx.moveTo(points[i].x, points[i].y);
          iCtx.lineTo(points[i + 1].x, points[i + 1].y);

          let life = 1 - (points[i].age / 50);
          // 曲線變細且有些微粗細變化
          iCtx.lineWidth = 15 * life + 1;
          iCtx.lineCap = 'round';
          iCtx.lineJoin = 'round';
          // 漸層消失的金�?          iCtx.strokeStyle = `rgba(184, 154, 106, ${life})`; // var(--gold)
          iCtx.stroke();
        }
      }
      requestAnimationFrame(loopInteraction);
    };
    loopInteraction();

  </script>
</body>

</html>
